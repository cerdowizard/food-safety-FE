import { useState } from "react";
import { CheckCircle } from "lucide-react";

interface ChecklistItem {
  id: string;
  title: string;
  description: string;
  category: string;
  isCompleted: boolean;
  priority: "high" | "medium" | "low";
  dueDate: string;
  points: number;
}

const Checklist = () => {
  const [checklist, setChecklist] = useState<ChecklistItem[]>([
    {
      id: "1",
      title: "Inspect weatherstripping and caulking around doors and windows",
      description: "Check for gaps, cracks or deterioration",
      category: "Maintenance checklist",
      isCompleted: false,
      priority: "high",
      dueDate: "2025-03-10",
      points: 3,
    },
    {
      id: "2",
      title: "Check for any signs of water damage from melting snow",
      description: "Inspect ceilings, walls and basement for water stains",
      category: "Maintenance checklist",
      isCompleted: false,
      priority: "high",
      dueDate: "2025-03-10",
      points: 3,
    },
    {
      id: "3",
      title: "Schedule regular pest control treatments",
      description: "Contact service provider for quarterly service",
      category: "Maintenance checklist",
      isCompleted: false,
      priority: "medium",
      dueDate: "2025-03-15",
      points: 3,
    },
    {
      id: "4",
      title: "Clean gutters and downspouts",
      description: "Remove debris and ensure proper drainage",
      category: "Maintenance checklist",
      isCompleted: false,
      priority: "medium",
      dueDate: "2025-03-12",
      points: 3,
    },
    {
      id: "5",
      title: "Inspect roofing for winter damage",
      description: "Look for missing shingles or damaged flashing",
      category: "Maintenance checklist",
      isCompleted: false,
      priority: "high",
      dueDate: "2025-03-11",
      points: 3,
    },
    {
      id: "6",
      title: "Flush and inspect water heater",
      description: "Remove sediment and check for proper operation",
      category: "Maintenance checklist",
      isCompleted: false,
      priority: "medium",
      dueDate: "2025-03-18",
      points: 3,
    },
  ]);

  const [activeTab, setActiveTab] = useState<"todo" | "completed">("todo");


  const handleCheckItem = (id: string) => {
    setChecklist(items =>
      items.map(item =>
        item.id === id ? { ...item, isCompleted: !item.isCompleted } : item
      )
    );
  };

  const completedTasks = checklist.filter(item => item.isCompleted);
  const todoTasks = checklist.filter(item => !item.isCompleted);
  const displayedTasks = activeTab === "todo" ? todoTasks : completedTasks;

  // Calculate score based on completed tasks
  const score = completedTasks.length;

  // Determine gauge color segments based on completion percentage
  const completionPercentage = (completedTasks.length / checklist.length) * 100;

  return (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        {/* Location */}


        {/* Dashboard Overview */}
        <div className="mb-8 grid grid-cols-1 md:grid-cols-2 gap-6">
          {/* Score Gauge */}
          <div className="bg-white rounded-xl shadow-sm p-6 flex items-center">
            <div className="relative w-40 h-20 overflow-hidden">
              <div className="w-40 h-40 relative">
                {/* Gauge background */}
                <svg className="w-full h-full" viewBox="0 0 100 50">
                  <path
                    d="M 10,50 A 40,40 0 0,1 90,50"
                    fill="none"
                    stroke="#e5e7eb"
                    strokeWidth="20"
                  />
                  {/* Red segment */}
                  <path
                    d="M 10,50 A 40,40 0 0,1 26,20"
                    fill="none"
                    stroke="#dc2626"
                    strokeWidth="20"
                  />
                  {/* Yellow segment */}
                  <path
                    d="M 26,20 A 40,40 0 0,1 50,10"
                    fill="none"
                    stroke="#d97706"
                    strokeWidth="20"
                  />
                  {/* Light green segment */}
                  <path
                    d="M 50,10 A 40,40 0 0,1 74,20"
                    fill="none"
                    stroke="#65a30d"
                    strokeWidth="20"
                  />
                  {/* Dark green segment */}
                  <path
                    d="M 74,20 A 40,40 0 0,1 90,50"
                    fill="none"
                    stroke="#15803d"
                    strokeWidth="20"
                  />
                </svg>
                <div className="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 text-center">
                  <div className="text-xs text-gray-500">SCORE</div>
                  <div className="text-3xl font-bold">{score}</div>
                </div>
              </div>
            </div>
            <div className="ml-6 flex-1">
              <h3 className="text-xl font-medium">Task Completion History</h3>
              <div className="flex items-center justify-between mt-2">
                <div className="text-gray-700">March 2025</div>
                <div className="text-green-600">
                  {completedTasks.length} tasks
                </div>
              </div>
            </div>
          </div>

          {/* Recent Activity */}
          <div className="bg-white rounded-xl shadow-sm p-6">
            <h3 className="text-xl font-medium">Recent activity</h3>
            <div className="mt-2 text-gray-700">
              {completedTasks.length > 0 ? (
                <ul className="space-y-2">
                  {completedTasks.slice(0, 3).map(task => (
                    <li key={task.id} className="flex items-center">
                      <CheckCircle className="h-4 w-4 text-green-500 mr-2" />
                      <span className="text-sm">{task.title}</span>
                    </li>
                  ))}
                </ul>
              ) : (
                <p>No recent activities</p>
              )}
            </div>
          </div>
        </div>

        {/* Tabs */}
        <div className="flex justify-between items-center mb-6">
          <div className="flex items-center">
            <button
              className="relative text-lg mr-4"
              onClick={() => setActiveTab("todo")}
            >
              All
              <svg
                xmlns="http://www.w3.org/2000/svg"
                className="h-5 w-5 inline-block ml-1"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  strokeWidth={2}
                  d="M19 9l-7 7-7-7"
                />
              </svg>
            </button>
          </div>
          <div className="flex space-x-2">
            <button
              className={`py-2 px-4 rounded-full text-sm font-medium ${
                activeTab === "todo"
                  ? "bg-purple-700 text-white"
                  : "bg-white text-gray-700"
              }`}
              onClick={() => setActiveTab("todo")}
            >
              To Do
            </button>
            <button
              className={`py-2 px-4 rounded-full text-sm font-medium ${
                activeTab === "completed"
                  ? "bg-white text-gray-700"
                  : "bg-white text-gray-700 hover:bg-gray-100"
              }`}
              onClick={() => setActiveTab("completed")}
            >
              Completed
            </button>
          </div>
        </div>

        {/* Checklist Cards */}
        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          {displayedTasks.map(item => (
            <div
              key={item.id}
              className="bg-white rounded-xl shadow-sm overflow-hidden"
            >
              <div className="px-6 py-4 bg-purple-50 text-sm font-medium text-purple-700">
                {item.category}
              </div>
              <div className="p-6">
                <div className="flex items-start">
                  <input
                    type="checkbox"
                    checked={item.isCompleted}
                    onChange={() => handleCheckItem(item.id)}
                    className="h-5 w-5 mt-1 rounded border-gray-300"
                  />
                  <div className="ml-3 flex-1">
                    <p
                      className={`text-base font-medium ${
                        item.isCompleted
                          ? "text-gray-400 line-through"
                          : "text-gray-900"
                      }`}
                    >
                      {item.title}
                    </p>
                    <p className="mt-1 text-sm text-gray-500">
                      {item.description}
                    </p>

                    <div className="mt-4 flex items-center justify-between">
                      <div className="flex items-center">
                        <span className="text-green-600 text-sm font-medium">
                          +{item.points} points
                        </span>
                        <span className="mx-2 text-gray-300">•</span>
                        <span className="text-gray-600 text-sm">Priority</span>
                      </div>
                    </div>

                    <div className="mt-3 flex space-x-2">
                      <button className="text-sm text-purple-700">
                        Add expense
                      </button>
                      <span className="text-gray-300">|</span>
                      <button className="text-sm text-purple-700">
                        View expense
                      </button>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};

export default Checklist;
